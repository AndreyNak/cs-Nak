<div class="mt-5">
  <div class="d-flex">
    <h3><%= link_to "< Back", admin_lessons_path, class: "text-decoration-none text-white me-2" %></h3>
    <h3><%= link_to "Edit", edit_admin_lesson_path(lesson.id), class: "text-decoration-none text-white" %></h3>
  </div>
  <div class="mt-5 lesson-show">
    <h1 class="title"><%= lesson.title %></h1>
    <div class="content">
      <%= sanitize lesson.content %>
    </div>
  </div>
</div>
